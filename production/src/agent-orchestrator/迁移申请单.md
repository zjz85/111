# 双区切换申请单：agent-orchestrator 迁入生产区

| 项 | 内容 |
|---|---|
| 申请日期 | 2026-08-09 |
| 探索区路径 | `exploration/agent-orchestrator` |
| 目标生产区路径 | `production/src/agent-orchestrator` |
| 接口契约 | ☑ 已完成（`production/src/agent-orchestrator/CONTRACT.md`） |
| 复杂度自查 | ☑ 通过（index.ts 已拆分，职责收敛） |
| 测试覆盖 | ☑ 核心路径（scope-fastpath / pr-meta / artifacts 共 19 测试） |
| 从探索区独立 | ☑ 是（依赖生产区共享库，不依赖探索区其它代码） |

**结论：** ☑ 通过
**原因：** 4 条准入标准全部满足，详见下方自查明细。

---

## 一、接口契约完整（标准 1）

- 新增 `production/src/agent-orchestrator/CONTRACT.md`，按 `接口契约.md` 模板定义：
  - 11 个模块的职责边界（核心职责 / 不做什么）
  - 接口签名（CLI 入口、runAgent、analyzePr、各 Agent 工具）
  - 输入参数 / 输出结构 / 异常语义
- 核心模块均有明确入参、返回值、异常处理

## 二、复杂度审查通过（标准 2）

- 原始 `index.ts` 上帝函数（编排+报告+记录+监控混杂），已拆分：
  - `index.ts`（纯编排流程）
  - `utils/pr-meta.ts`（审计字段提取）
  - `utils/scope-fastpath.ts`（SCOPE-001 快速通道）
  - `utils/artifacts.ts`（报告/记录/打回/告警写入）
- 符合核心区"单一职责、规则唯一、可测可解释、依赖轻量"要求
- 无重复逻辑：共享库（record-store/diff-splitter/mcp-client）保持生产区单实例

## 三、测试覆盖达标（标准 3）

新增 3 个测试文件，19 个测试：

| 测试文件 | 覆盖场景 |
|---|---|
| `__tests__/scope-fastpath.test.ts` | 有效行数提取、阈值命中/边界/未命中、报告生成 |
| `__tests__/pr-meta.test.ts` | 审计字段提取、fallback 容错、空值处理 |
| `__tests__/artifacts.test.ts` | 记录写入、打回条件、误判告警、CI/本地路径 |

覆盖核心路径 + 边界（阈值恰好=500）+ 失败（解析失败 fallback）。

## 四、依赖已收敛（标准 4）

- 依赖生产区共享库（`../orchestrator/utils/*`、`../shared/types`），保持单实例
- **不依赖探索区其它代码**（仅本地测试用 `exploration/sample-prs` 样例数据）
- 依赖仅 `@anthropic-ai/sdk` + `@modelcontextprotocol/sdk`

---

## 迁移改动清单

| 类型 | 内容 |
|---|---|
| 新增 | `production/src/agent-orchestrator/`（源文件 + CONTRACT.md + 测试） |
| 删除 | `exploration/agent-orchestrator/`（原探索区版本） |
| 修改 | `.github/workflows/pr-review.yml`（路径指向生产区） |
| 修改 | mcp-server 移到根目录 `mcp-server/`，同步引用路径 |
