# PR自动初评机器人 — 实施计划

## 已完成 ✅

**MCP Server（规则知识库）** — `production/src/mcp-server/`

| 内容 | 数量 | 说明 |
|------|------|------|
| Resource | 8 个 | 6 份规则文件 + 全部汇总 + 项目设计文档 |
| Tool | 5 个 | 规则检索、复杂度检查、架构检查、依赖检查、安全检查 |
| 规则 | 15 条 | 从 `team-rules/` 下 6 份 Markdown 文件解析 |

已验证：服务器启动正常、规则解析正确、各 Tool 返回正确。

---

## Phase 2：评审主程序 `orchestrator/`

### 目标

从本地 sample-prs 读 PR → 调 DeepSeek API 打分 → 规则引擎判定 → 输出 Markdown 报告

### 要做的东西

```
production/src/orchestrator/
  index.ts                          # 主入口，串联 3 个阶段
  pipeline/
    stage1-fetch.ts                 # 从本地 sample-prs 读取 PR（diff + metadata）
    stage2-review.ts                # 调 DeepSeek API 做 4 维度评分
    stage3-decide.ts                # 规则引擎：读评分 → 判定通过/打回/转人工
  utils/
    diff-splitter.ts                # 大 diff 分片（>500 行按文件边界切，每片 ≤300 行）
    mcp-client.ts                   # 连接本地 MCP Server，读取规范文档
    report-formatter.ts             # 把结构化结果转成 Markdown 报告
```

### 2 个 Skill（prompt 模板）

```
production/skills/
  four-dim-score/prompt.md          # 告诉 DeepSeek：你按 4 维打分，扣分标注行号，输出 JSON
  review-report/prompt.md           # 告诉 DeepSeek：把评分转成开发者能看的 Markdown 报告
```

**Skill 不是神秘的东西**——就是一段写好的 AI 指令文本，评审时程序读这个文件，拼上 PR diff，一起发给 DeepSeek。

### 流程

```
pr-001 的 diff → Stage1 读取 + 判断是否分片
                 → Stage2 读 prompt 模板 + 读 MCP 规范 + 拼上 diff → 发 DeepSeek
                 → DeepSeek 返回 JSON（各维度得分 + 问题清单 + 行号）
                 → Stage3 匹配 15 条规则 → 输出: 通过 / 打回 / 转人工
                 → 格式化成 Markdown 报告
```

### 验证

用 `exploration/sample-prs/` 下 20 个 PR 跑一遍：

| 预期结果 | 例子 |
|---------|------|
| 通过 | PR-001（小改动+测试）、PR-010（等价重构）、PR-015（补测试） |
| 打回 | PR-005（560行重写）、PR-002（Controller直连DB）、PR-007（any绕过类型） |
| 转人工 | PR-014（复杂度边界）、PR-019（缺rollout说明） |

---

## Phase 3：双模型评审 + CI 集成

### 双模型

用 DeepSeek 两个模型交叉验证：

| 角色 | 模型 | 特点 |
|------|------|------|
| 主审 | `deepseek-chat` | 速度快、成本低 |
| 复审 | `deepseek-reasoner` | 带推理链、思维更严谨 |

**触发复审的条件**（满足任一）：
1. 改动涉及金额/支付/退款/认证等敏感词
2. 任一维度得分 = 3 分（边界，吃不准）
3. 主审返回 `uncertain`

**双模型结论处理**：
- 一致 → 采纳
- 不一致 → 强制转人工 Reviewer

**双模型记录**：每次双审都记到 `data/dual-model-records.json`，字段包括：
- 哪个 PR、触发原因、主审结论、复审结论、是否一致、最终判定

> 对应原需求"双模型记录：哪些 PR 用了双审、为什么？"

### CI 集成（GitHub Actions）

`.github/workflows/pr-review.yml`：
```yaml
on:
  pull_request:
    types: [opened, synchronize, reopened]   # PR 创建/更新/重开时自动跑

jobs:
  review:
    steps:
      - 拉代码 → 装依赖 → 跑 npm run review → 结果贴回 PR 评论区
```

---

## Phase 4：Subagent 复审

### 要做什么

在主程序之外，配一个独立的 **Subagent**（在 Claude Code 里用 `/agent` 或 Agent SDK 定义），用于**人工触发**的深度复审：

- 当 Reviewer 对主审结果不认同时，手动调 Subagent 做第三次独立评审
- Subagent 用与主审不同的模型（或不同的 prompt 策略），提供"第二意见"

### Subagent 定义

```
subagents/codex-reviewer/SUBAGENT.md
```

包含：角色设定、4 维评分标准、必须引用规范原文、必须标注行号

> 对应原需求"至少 1 个 Subagent：用 Codex 作为复审"

---

## Phase 5：Hooks（Claude Code 本地开发用）

在 `.claude/settings.json` 中配 3 个 Hook：

| Hook | 触发时机 | 做什么 |
|------|---------|--------|
| **PreToolUse** | `git push` 之前 | 自动跑本地预检脚本，检查变更是否可能违规 |
| **PostToolUse** | `Write/Edit` 文件后 | 弹提示："文件已修改，记得跑本地评审 `npm run review:local`" |
| **Notification** | 评审完成后 | 把评审结果追加到本地日志文件，积累误判率数据 |

> 对应原需求"至少 3 个 Hook"

---

## 5 个核心问题的解决

| 问题 | 方案 |
|------|------|
| **1. PR diff 太大？** | 三级分片：① 标记为生成文件的跳过 ② 按文件边界切片，每片 ≤300 行 ③ 单文件超大按 hunk 边界切；所有切片独立评审后聚合 |
| **2. 开发者反对打回？** | 在 PR 评论中回复 `@bot dispute` → 机器人自动打 `label:bot-disputed` → 通知人工 Reviewer 介入 → 人工最终裁决写入记录 |
| **3. 误判率监控？** | 每次评审全量记录到 `ReviewRecord`；跟踪 `human_override_rate`（人工推翻/自动打回），>15% 触发规则阈值调整；`config/rules.json` 支持热更新和 git 回滚 |
| **4. 新规范不影响在途 PR？** | 规范版本化，按 PR 创建时间匹配对应版本；规范变更时自动对全部 Open PR 做"影子评审"（只记录不发布），提前预警 |
| **5. 机器人自身 review？** | 机器人代码走正常 PR 流程 + 机器人自评 + 每 2 周人工抽样 10 份评审记录做 meta-review；一致性 >90% 后降低频率 |

---

## 项目文件总览

```
production/
  src/
    mcp-server/          ✅ 已完成 — 规则知识库
    orchestrator/           ← Phase 2 — 评审主程序
      index.ts
      pipeline/
        stage1-fetch.ts
        stage2-review.ts
        stage3-decide.ts
      utils/
        diff-splitter.ts
        mcp-client.ts
        report-formatter.ts
    skills/                 ← Phase 2 — prompt 模板
      four-dim-score/prompt.md
      review-report/prompt.md
    subagents/              ← Phase 4 — 复审 Subagent
      codex-reviewer/SUBAGENT.md
    shared/
      types.ts
  data/
    dual-model-records.json  ← Phase 3 — 双模型记录
    review-records.json      ← Phase 5 — 评审全量记录

.claude/
  settings.json              ← Phase 5 — 3 个 Hook

.github/workflows/
  pr-review.yml              ← Phase 3 — CI 自动触发
```

---

## 进度一览

| 阶段 | 状态 | 产出 |
|------|------|------|
| Phase 1 MCP Server | ✅ 完成 | 8 Resource + 5 Tool，15 条规则 |
| Phase 2 评审主程序 | 🔜 下一步 | orchestrator + 2 个 Skill + 20 个 PR 跑通 |
| Phase 3 CI + 双模型 | ⏳ | GitHub Actions + deepseek-chat/reasoner 交叉验证 |
| Phase 4 Subagent | ⏳ | Codex 复审 Subagent 定义 |
| Phase 5 Hooks + 监控 | ⏳ | 3 个 Hook + 误判率统计 + 争议处理 |
