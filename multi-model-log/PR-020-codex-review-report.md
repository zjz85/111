---
name: Codex-reviewer 复审报告
pr: PR-020
reviewer: Codex CLI (DeepSeek)
review_time: 2026-08-09
---

## Codex-reviewer 专项复审

| 定位 | 问题 | 规范引用 | 危害说明 | 严重等级 |
|---|---|---|---|---|
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方传入空值（如从表单或接口获取未校验的字段）会导致运行时崩溃，属于空指针/未定义类问题 | 打回 |
| `format-phone.ts:41` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | 调用方