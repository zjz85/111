# PR 自动初审报告 — PR-020

| 元信息 | 内容 |
|---|---|---|
| PR 编号 | PR-020 |
| 标题 | Bot-test: format-phone add validation/masking |
| 作者 | zjz85 |
| 判定 | **拒绝** |

## 综合评分

#### 四维评审

| 维度 | 权重 | 得分 | 加权分 | 状态 |
|---|---|---|---|---|
| 正确性 | 40% | 4 | 1.60 | ❌ 存在打回项 |
| 可维护性 | 25% | 3 | 0.75 | ✅ |
| 可读性 | 20% | 4 | 0.80 | ✅ |
| 可演进性 | 15% | 4 | 0.60 | ✅ |
| **加权总分** | — | — | **3.75** | ❌ 有打回项 |

> 加权总分 3.75 ≥ 3.0，但 Codex 专项复审在正确性维度发现打回级空指针问题，按规则存在 reject severity 打回项即判定打回。

## 规范检查

#### MCP 检查摘要

| 检查工具 | 结果 | 命中 |
|---|---|---|
| check_complexity | ✅ 通过 | 无 |
| check_architecture | ✅ 通过 | 无 |
| check_security | ✅ 通过 | 无 |

#### 高风险命中

| 项目 | 内容 |
|---|---|---|
| highRiskHit | true |
| highRiskTypes | core_interface |
| 复审工具 | Codex-reviewer |

## 问题清单

#### 打回项（Codex 专项复审）

| 定位 | 问题 | 规范引用 | 严重等级 |
|---|---|---|---|
| `[exploration/bot-test/format-phone.ts，41]` | `maskPhoneNumber` 中 `input.trim()` 在 `input` 为 `null` 或 `undefined` 时抛出 TypeError | SEC-002 | ❌ 打回 |

> `maskPhoneNumber` 未像 `isValidPhoneNumber` 那样对 `input` 做空值守卫，直接调用 `input.trim()`。调用方传入空值（如从表单或接口获取未校验字段）会导致运行时崩溃，属于空指针/未定义类问题。建议在函数入口增加 `if (!input) return input;` 或等价守卫。

#### 建议项（评审 Agent）

| 定位 | 问题 | 规范引用 | 严重等级 |
|---|---|---|---|
| `[exploration/bot-test/format-phone.ts，43]` | `maskPhoneNumber` 无效输入返回未 trim 原始值，行为不一致 | CORRECT-001 | 建议 |
| `[production/data/review-records.json，100]` | 评审记录 highRiskHit 为 false 与实际 true 不符，prTitle 为 'PR 自动初审' 与实际标题不符 | DATA-001 | 建议 |
| `[exploration/bot-test/format-phone.ts，58]` | 遗留调试注释 '// 触发重新评审' | READ-001 | 建议 |
| `[exploration/bot-test/format-phone.ts，52]` | `PhoneMaskResult` 类型为死代码（导出但未使用） | MAINT-001 | 建议 |
| `[exploration/bot-test/format-phone.ts，34]` | 新增业务逻辑缺少测试 | RULE-TEST-001 | 建议 |

## 最终判定

**拒绝**（打回）。

- Codex 专项复审在正确性维度发现打回级空指针问题：`maskPhoneNumber` 对 `null`/`undefined` 输入调用 `input.trim()` 会抛出 TypeError（SEC-002）。
- `review-records.json` 中 `highRiskHit: false` 与预处理结果 `highRiskHit: true`（core_interface）矛盾，且 `prTitle` 与实际标题不符，污染后续 multi-model-log 统计与审计。
- 历史记录显示 PR-020 曾被错误落盘为 pass（weightedScore 0），印证了该数据一致性问题。

**修复建议**：

- 在 `maskPhoneNumber` 入口增加空值守卫，避免 `null`/`undefined` 输入崩溃。
- 修正 `review-records.json` 中 PR-020 记录的 `highRiskHit` 与 `prTitle` 为真实值。
- 移除遗留调试注释 `// 触发重新评审`，删除未使用的 `PhoneMaskResult` 类型，并补充边界测试。