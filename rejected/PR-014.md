# PR 自动初审报告 — PR-014（Bot-test: settlement rewrite）

| 元信息 | 值 |
|---|---|---|
| PR 编号 | PR-014 |
| 标题 | Bot-test: settlement rewrite (expected SCOPE-001 reject) — clean diff |
| 作者 | zjz85 |
| 判定 | ❌ 打回 |

## 综合评分

| 维度 | 权重 | 得分 | 说明 |
|---|---|---|---|
| 正确性 | 40% | 2 | 支付签名弱哈希、多步操作无事务、对账逻辑硬编码错误 |
| 可维护性 | 25% | 2 | 877 行单文件堆叠约 60 个函数，无测试 |
| 可读性 | 20% | 2 | 大量魔法数字（费率、阈值、运费） |
| 可演进性 | 15% | 2 | 领域层直接依赖 ORM，单文件难以扩展 |
| **加权总分** | — | **2.0** | 低于 3.0 阈值 |

## 规范检查

| 检查项 | 结果 | 说明 |
|---|---|---|
| check_complexity | ❌ 拒绝 | SCOPE-001：有效行数 877 行超过 500 行阈值，应拆分为多个小型 PR；READ-001 预警 |
| check_architecture | ✅ 通过 | 未检测到架构边界违规 |
| check_security | ✅ 通过 | 未检测到安全/隐私违规（静态扫描层面） |

## 问题清单

| 严重等级 | 定位 | 问题 | 规范引用 |
|---|---|---|---|
| ❌ 打回 | `[settlement-rewrite.ts，全文件]` | 功能性 diff 877 行超过 500 行阈值，未拆分为多个小型 PR | SCOPE-001 |
| ❌ 打回 | `[settlement-rewrite.ts，246-248]` | `verifyPaymentCallback` 使用非加密哈希（`hash \|= 0` 32 位整数哈希）且 `sign === expected` 非恒定时间比较，支付回调签名可被伪造 | SEC-001 |
| ❌ 打回 | `[settlement-rewrite.ts，262-264]` | `confirmPayment` 先更新 payment 为 SUCCESS 再更新 order 为 PAID，两步无事务包裹，失败时状态不一致 | ARCH-001 |
| ❌ 打回 | `[settlement-rewrite.ts，278-280]` | `cancelOrder` 先释放库存再更新订单状态，无事务包裹，失败时导致超卖 | ARCH-001 |
| ❌ 打回 | `[settlement-rewrite.ts，340-342]` | `buildReconciliation` 中 `createdAt.slice(0, 10)` 假设为字符串，Prisma 可能返回 Date 对象，运行时报 TypeError | ARCH-001 |
| ❌ 打回 | `[settlement-rewrite.ts，355-357]` | `buildReconciliation` 中 `discountAmount` 硬编码为 0，未从实际折扣计算，对账结果失真 | LOGIC-001 |
| ❌ 打回 | `[settlement-rewrite.ts，355-357]` | `buildReconciliation` 中 `settlementAmount = netAmount * 0.97` 硬编码费率，与 `calcPlatformFee` 的 0.006 不一致 | LOGIC-001 |
| ⏸ 建议 | `[settlement-rewrite.ts，296-298]` | `applyRefund` 中 `order.amount` 可能为 null/undefined，直接写入退款金额 | ARCH-001 |
| ⏸ 建议 | `[settlement-rewrite.ts，330-332]` | `executeRefund` 中 `refund.amount` 可能为 null/undefined | ARCH-001 |
| ⏸ 建议 | `[settlement-rewrite.ts，355-357]` | `buildReconciliation` 中 `netAmount` 用退款金额直接减退款订单原始金额，逻辑混淆 | LOGIC-001 |
| ⏸ 建议 | `[settlement-rewrite.ts，355-357]` | `buildReconciliation` 中 `rows.sort` 日期相等时返回 1，排序不稳定 | LOGIC-001 |
| ⏸ 建议 | `[settlement-rewrite.ts，58]` | 领域层直接 `new PrismaClient()` 依赖 ORM，建议通过仓储层隔离 | ARCH-002 |
| ⏸ 建议 | `[settlement-rewrite.ts，全文件]` | 大量魔法数字（运费、免邮阈值、人工审核阈值、风控阈值、费率、佣金率） | READ-001 |
| ⏸ 建议 | `[settlement-rewrite.ts，全文件]` | 未包含任何测试用例 | TEST-001 |

## 最终判定

**❌ 打回**

本 PR 存在多项打回项，判定为**打回**：

- MCP 规模检查直接命中 **SCOPE-001**（877 行 > 500 行阈值），属直接打回项。
- Codex-reviewer 专项复审命中 **SEC-001**（支付签名弱哈希可伪造）、**ARCH-001**（多步写操作无事务、Date 类型误用）、**LOGIC-001**（对账折扣与费率硬编码错误）等多项打回项。
- 四维加权总分 **2.0**，低于 3.0 阈值。

建议：将结算模块拆分为多个小型 PR（金额计算、支付、退款、对账、风控等独立提交），并修复支付签名校验、事务包裹与对账逻辑后再行提交。