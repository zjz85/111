# PR 自动初审报告 — PR-012（Bot-test: user query controller (expected reject)）

**判定：❌ 拒绝（打回）** ｜ 作者：zjz85 ｜ 分支：test/arch-reject-bot → feat/agent-ci

## 综合评分

| 维度 | 权重 | 得分 | 加权 | 结论 |
|---|---|---|---|---|
| 正确性 | 40% | 3 | 1.20 | ⏸ 待补充 |
| 可维护性 | 25% | 2 | 0.50 | ❌ 打回 |
| 可读性 | 20% | 4 | 0.80 | ✅ 通过 |
| 可演进性 | 15% | 2 | 0.30 | ❌ 打回 |
| **加权总分** | — | — | **2.90** | ❌ 低于 3.0 |

## 规范检查

### MCP 检查摘要

| 检查工具 | 结果 | 命中 |
|---|---|---|
| check_complexity | ✅ 通过 | 无 |
| check_architecture | ❌ 拒绝 | ARCH-001（severity=reject） |
| check_security | ✅ 通过 | 无 |

**MCP 直接打回**：`check_architecture` 命中 `ARCH-001`（Controller 疑似直接访问数据库，必须调用 application service），severity=reject，直接打回。

## 问题清单

| 严重等级 | 规则 | 定位 | 问题描述 |
|---|---|---|---|
| ❌ reject | ARCH-001 | `[exploration/bot-test/user-query-controller.ts，10]` | Controller 构造函数直接 `new PrismaClient()`，绕过 userService 直连数据库，破坏分层架构。 |
| ❌ reject | ARCH-001 | `[exploration/bot-test/user-query-controller.ts，10]` | Controller 与数据访问层强耦合，无法替换数据源或注入 mock 进行单元测试，扩展点封闭。 |
| ⏸ suggest | CORR-001 | `[exploration/bot-test/user-query-controller.ts，19]` | 外部输入 `id` 未做非空及格式校验，非法输入可能导致异常或非预期查询。 |
| ⏸ suggest | CORR-002 | `[exploration/bot-test/user-query-controller.ts，19]` | 数据库异常未捕获或包装，异常直接向上抛出，调用方难以获得可读错误信息。 |

### 高风险代码专项复审（Codex-reviewer）

| 定位 | 问题 | 规范引用 | 严重等级 |
|---|---|---|---|
| `[user-query-controller.ts，21]` | `id` 参数未做空值/类型校验，传入 `undefined`/`null` 时 Prisma 可能抛异常 | 边界情况 | ⏸ 建议 |
| `[user-query-controller.ts，21]` | `findUnique` 可能返回 `null`，调用方直接访问属性会触发空指针 | 空指针 | ⏸ 建议 |
| `[user-query-controller.ts，21]` | 数据库查询异常未捕获，无降级或重试机制 | 异常处理 | ⏸ 建议 |

> 注：未发现 SQL 注入、逻辑错误、竞态条件、语法错误问题。核心问题 ARCH-001 属架构维度，不在正确性 7 类职责范围内。

## 最终判定

**❌ 拒绝（打回）**

打回原因：

- **MCP 直接打回**：`check_architecture` 命中 `ARCH-001`（severity=reject），Controller 直接实例化 PrismaClient 并绕过 userService 直连数据库，违反分层架构规范。
- **加权总分 2.90 < 3.0**：可维护性（2 分）与可演进性（2 分）维度均存在打回项，Controller 与数据访问层强耦合，无法测试、无法替换数据源。

修复建议：

- 改为依赖注入 `userService`，由 Controller 调用 `userService.get(id)` 获取数据，Controller 仅依赖抽象接口。
- 在 service 层统一封装数据库异常并转换为业务错误。
- 在入口处对 `id` 做非空及格式校验，并明确错误语义。

该 PR 为测试样例，与 PR 描述中「预期打回」一致，reject 链路验证符合预期。