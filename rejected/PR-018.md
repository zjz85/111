# PR 自动初审报告 — PR-018

| 项目 | 内容 |
|---|---|---|
| PR 编号 | PR-018 |
| 标题 | Bot-test: user-controller add searchUserByPhone (expected ARCH-001 + SEC-002 reject) |
| 作者 | zjz85 |
| 判定 | **拒绝** |

## 综合评分

| 维度 | 权重 | 得分 | 加权分 | 状态 |
|---|---|---|---|---|
| 正确性 | 40% | 2 | 0.80 | ❌ 打回 |
| 可维护性 | 25% | 3 | 0.75 | ❌ 打回 |
| 可读性 | 20% | 4 | 0.80 | ✅ 通过 |
| 可演进性 | 15% | 2 | 0.30 | ⏸ 建议 |
| **加权总分** | — | — | **2.70** | ❌ 低于 3.0 |

## 规范检查

### MCP 检查摘要

| 检查项 | 结果 | 命中 |
|---|---|---|
| check_complexity | ✅ 通过 | 无 |
| check_architecture | ✅ 通过 | 无 |
| check_security | ✅ 通过 | 无 |

### Codex 专项复审（db_operation）

| 定位 | 问题 | 规范引用 | 严重等级 |
|---|---|---|---|
| `[exploration/bot-test/user-controller.ts，49]` | `phone` 参数未做格式验证（空字符串、非法字符），直接传入数据库查询 | SEC-002 | 建议 |
| `[exploration/bot-test/user-controller.ts，49]` | `findFirst` 返回 `null` 时调用方未处理空值场景，可能引发空指针 | 无 | 建议 |
| `[exploration/bot-test/user-controller.ts，49]` | 未对 `phone` 做长度限制，超长输入可能导致查询性能下降 | SEC-002 | 建议 |

**Codex 复审结论**：未发现 SQL 注入（Prisma 参数化查询已规避）、逻辑错误、竞态条件、语法错误等严重正确性问题；主要问题集中在输入验证与空值处理，均为建议级。

## 问题清单

| 定位 | 严重等级 | 规范 | 问题描述 |
|---|---|---|---|
| `[exploration/bot-test/user-controller.ts，49]` | ❌ reject | SEC-002 | 外部输入 `phone` 未做格式验证（空值、格式、长度）直接传入数据库查询，存在无效查询与潜在注入风险 |
| `[exploration/bot-test/user-controller.ts，49]` | ❌ reject | ARCH-001 | Controller 直连数据库，违反『Controller 不得直接访问数据库，必须调用 application service』分层规范 |
| `[exploration/bot-test/user-controller.ts，49]` | ⏸ suggest | ARCH-001 | Controller 直接依赖 db 层，架构耦合限制可演进性，建议通过 service 层抽象数据访问 |

## 最终判定

**拒绝**。

加权总分 **2.70**，低于 3.0 阈值；正确性维度存在 SEC-002 打回项，可维护性维度存在 ARCH-001 打回项。

`searchUserByPhone` 方法违反两条硬性规范：

- **SEC-002**：外部输入 `phone` 未做任何格式验证（空值、格式、长度）即直接传入数据库查询。
- **ARCH-001**：Controller 直接调用 `this.db.user.findFirst` 访问数据库，未下沉到 application service 层。

虽然 PR 描述表明这是用于验证 CI 打回机制的测试 PR（Bot-test），但评审基于代码本身是否符合团队硬性规范，代码确实违反两条红线规则，应判定打回。

**建议**：将数据库查询逻辑下沉到 service 层，并在方法入口对 `phone` 做格式校验（非空、手机号正则、长度限制），校验失败时返回明确错误。